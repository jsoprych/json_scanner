# Build artifacts
/bin/
/scanner
*.exe
*.test
*.out
coverage.*

# Secrets / local config
.env
.env.*
*.env

# Local copies of the warehouse (read the pipeline's DB in place instead)
*.db
*.db-*
*.sqlite
*.sqlite3

# Go workspace — local/dev only
go.work
go.work.sum

# OS / editor
.DS_Store
*.swp
.idea/
.vscode/

# Claude Code — local settings / transient temp files
.claude/settings.local.json
.claude/*.tmp.*
